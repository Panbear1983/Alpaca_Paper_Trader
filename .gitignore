# credentials — never commit
.env

# Python cache
__pycache__/
*.pyc
*.pyo

# virtualenv
venv/
.venv/

# scheduler runtime state
.report_schedule_state.json

# macOS
.DS_Store

# large derived / cache files
backtest_cache.json

# report output — keep .md reports, exclude binaries and logs
reports/*.png
reports/*.ogg
reports/*.aiff
reports/*.mp3
reports/*.log
reports/cron.log

# launchd logs
.logs/

# autonomous-edit sandbox artifacts (Stage 2 propose/apply)
proposals/

# runtime state — regenerated each run, mirrors DIFF_SKIP in propose_change.py.
# Never publish live wallet state to a public repo.
.copied_trades*.json
.position_state*.json
.swing_state*.json
.intraday_state*.json
.isr_alpha_state*.json
.sector_cache.json

# Wanna Buffet trading diary — live wallet state, same policy as above
diary/
.trading_schedule_state*.json
.event_watcher_state.json
.event_watcher.lock
.tui.lock
pool_state.json
performance_log.json
politician_history.json
review_log.json
vetting_log.json
isr2026_signals.json

# generated output
logs/
reports/*.md
*.bak_pre_split
*.backup

# editor/agent backup copies — never commit (many *.backupN / .bak / .fixed piled up in Aug 2026)
*.backup*
*.bak
*.bak?
*.orig
*.fixed
*.before_*

# more runtime state
.regime_cache.json
positions.json
